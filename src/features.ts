/**
 * Ids of the original in-IDE simulations built into IdeLayout. Tour stops
 * reference these via `sim`; all display copy now lives in src/tour.ts.
 */
export type FeatureId =
  | 'agent'
  | 'context'
  | 'modes'
  | 'diffs'
  | 'tab'
  | 'inline-edit'
  | 'browser'
  | 'automations'
